# SayStory releases

Installer downloads and the in-app updater feed (`latest.json`) for [SayStory](https://github.com/3D-Stories/saystory) (source repo is private).

Grab the newest installer from **Releases**. The app updates itself from `releases/latest/download/latest.json`.
